<h2>予約内容の確認</h2>

<div>
  <p><strong>チェックイン日:</strong> <%= @reservation.check_in.to_s(:date_jp) %></p>
  <p><strong>チェックアウト日:</strong> <%= @reservation.check_out.to_s(:date_jp) %></p>
  <p><strong>宿泊日数:</strong> <%= (@reservation.check_out - @reservation.check_in).to_i / 1.day %>日</p>
  <p><strong>宿泊人数:</strong> <%= @reservation.number_of_guests %>人</p>
  <p><strong>支払い金額:</strong> <%= number_to_currency((@reservation.check_out - @reservation.check_in).to_i / 1.day * @reservation.number_of_guests * @room.price) %></p>
  <p>上記内容で正しければ「確定」ボタンを押してください</p>
</div>

  <%= form_with(model: @reservation, url: reservations_path, method: :post, local: true) do |f| %>
    <%= f.hidden_field :check_in, value: @reservation.check_in %>
    <%= f.hidden_field :check_out, value: @reservation.check_out %>
    <%= f.hidden_field :number_of_guests, value: @reservation.number_of_guests %>
    <%= f.hidden_field :room_id, value: @reservation.room_id %>
    <%= f.submit "予約を確定" %>
    <%= link_to "キャンセル", :back %>
  <% end %>
  