<h1>Rooms#index</h1>
<p>Find me in app/views/rooms/index.html.erb</p>

<% @rooms.each do |room| %>
    <%= attachment_image_tag room, :image %>
    <%= room.title %>
    <%= room.body %>
    <%= room.price %>
    <%= room.address %>
    <%= link_to room_path(room) do %>詳細はこちら<% end %>
<% end %>
